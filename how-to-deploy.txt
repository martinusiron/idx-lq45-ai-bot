# VPS Biznet
ssh root@YOUR_IP

# 1. Setup folder
mkdir -p /root/lq45-signal-bot/systemd
cd /root/lq45-signal-bot

# 2. Upload files (nano satu-satu atau git)

# 3. .env
nano .env  # TELEGRAM_TOKEN & CHAT_ID

# 4. Deploy
chmod +x deploy.sh
./deploy.sh

# 5. Monitor
journalctl -u lq45-signal-bot -f
